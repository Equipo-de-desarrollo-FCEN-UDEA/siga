import { GetDocumentPipe } from './get-document.pipe';

describe('GetDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
